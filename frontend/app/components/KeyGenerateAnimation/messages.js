/*
 * KeyGenerateAnimation Messages
 *
 * This contains all the text for the KeyGenerateAnimation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.KeyGenerateAnimation';

export default defineMessages({
  heading: {
    id: `${scope}.heading`,
    defaultMessage: 'Creating your credentials',
  },
});
