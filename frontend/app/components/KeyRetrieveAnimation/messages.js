/*
 * KeyRetrieveAnimation Messages
 *
 * This contains all the text for the KeyRetrieveAnimation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.KeyRetrieveAnimation';

export default defineMessages({
  heading: {
    id: `${scope}.heading`,
    defaultMessage: 'Retrieving credentials from keystore',
  },
});
